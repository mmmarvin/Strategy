#ifndef SCRIPT_EMBEDS_H
#define SCRIPT_EMBEDS_H

// Because sol2 is heavily templated, it would be better for compilation time
// to separate the embeddings of the classes, class wrappers, etc.
// into different source / header files. This header here is the one header to
// include all the lua embeddings

#endif // SCRIPT_EMBEDS_H
